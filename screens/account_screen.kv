<AccountSettings>:
    MDBoxLayout:
        orientation: "vertical"

        MDTopAppBar:
            type: "small"

            MDTopAppBarLeadingButtonContainer:
                MDActionTopAppBarButton:
                    icon: "arrow-left"
                    on_press:
                        app.root.current = root.go_back()

            MDTopAppBarTitle:
                text: "Account"

        BoxLayout:

        MDButton:
            style: "tonal"
            pos_hint: {"center_x": 0.5}
            on_press:
                root.on_log_out()
                app.root.current = root.go_to_signup_screen()

            MDButtonText:
                text: "Log out"

        BoxLayout:
